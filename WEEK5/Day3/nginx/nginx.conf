events {}
http {
    upstream backend_servers {
        server backend-alpha:3000;
        server backend-beta:3000;
    }
    server {
        listen 80;

        location /api/ {
            proxy_pass http://backend_servers;
        }
    }
}